import 'package:flutter/material.dart';

class CommunityMentorCard extends StatelessWidget {
  const CommunityMentorCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 96,
      height: 64,
      child: Card(),
    );
  }
}
